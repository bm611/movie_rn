import { Text, View } from "react-native";

const Saved = () => {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-center text-4xl text-primary font-bold">Saved</Text>
    </View>
  );
};

export default Saved;
